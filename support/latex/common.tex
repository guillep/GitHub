\stockustrade
\setbleed{.125in}
\setlrmarginsandblock{.75in}{*}{*}
\setulmarginsandblock{.75in}{.75in}{*}
\checkandfixthelayout
\typeoutlayout

\usepackage{polyglossia}

% extract info from git commit
% requires gitinfo2 2.0.7, and below fix needed until 2.0.8 released
%\usepackage[local,dirty=*]{gitinfo2}
\usepackage{xstring}% \IfEq and \StrCut
% a couple extensions...
%\makeatletter
%\newcommand\gitCommitInfo{%
%  \IfEq{\gitRel}{}
%    {commit \texttt{\gitAbbrevHash\gitDirty}}
%    {\IfEq{\gitRoff}{0}
%      {release \gitRel}
%      {modified since release \gitRel{} --- commit \texttt{\gitAbbrevHash\gitDirty}}}}
%  \newcommand\gitdate{\DTMusedate{gitdate}}
%\makeatother

\usepackage[normalem]{ulem} % for strikeout text (\sout{...})
\usepackage{graphicx}

\usepackage[% make hyperlinks look like normal text
  colorlinks=true,
  linkcolor=blue,
  urlcolor=blue
]{hyperref}
\usepackage[anythingbreaks]{breakurl}

% Lulu only supports PDF format version 1.3
\pdfminorversion=3

\usepackage{multicol}
\usepackage{multirow}

% a nice horizontal separator
\newcommand{\sectionline}{%
  \begin{center}\nointerlineskip
    \rule{0.233\linewidth}{.7pt}%
  \end{center}
}

% less space around lists
\tightlists

% Inline code
\newcommand\code[1]{\texttt{\textup{#1}}}

% Use minted for code blocks
%\usepackage{minted}

%\catcode`\*=11
%\makeatletter{}
%\let\listing\@undefined
%\let\endlisting\@undefined
%\let\listing*\@undefined
%\let\endlisting*\@undefined
%\makeatother{}
%\catcode`\*=12

%\newminted[listing]{smalltalk}{}

%\catcode`\*=11
%\let\listing\listing*
%\let\endlisting\endlisting*
%\catcode`\*=12

%\makeatletter{}
%\define@key{mylisting}{language}{\def\mylt@language{#1}}
%\setkeys{mylisting}{language=smalltalk}
%\renewenvironment{listing}[1][language=smalltalk]{%
%\setkeys{mylisting}{#1}
%\begin{minted}[linenos=true,breaklines,fontsize=\footnotesize]{\mylt@language}%
%}
%{%
%\end{minted}
%}
%\makeatother{}

\usepackage{verbments}
\plset{language=smalltalk,numbers=left}
\let\listing\pyglist
\let\endlisting\endpyglist

% highlighted paragraphs
\newenvironment{note}{%
  \begin{leftbar}\textbf{Note}\quad
}{\end{leftbar}}

\newenvironment{todo}{%
  \begin{leftbar}\textbf{To do}\quad
}{\end{leftbar}}

